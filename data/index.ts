import { Task, CronJob, LogEntry } from '@/types'

// Data now fetched from API — these exports kept for type compatibility
export const initialTasks: Task[] = []
export const cronJobs: CronJob[] = []
export const logEntries: LogEntry[] = []
